using UnityEngine;
using System.Collections;

public class button : MonoBehaviour {

	/*public void agi() {
		if (GameManager.gm.player.caracsPoints > 0) {
			GameManager.gm.player.stats ["agi"].value++;
			GameManager.gm.player.caracsPoints--;
		}
	}

	public void str() {
		if (GameManager.gm.player.caracsPoints > 0) {
			GameManager.gm.player.stats ["str"].value++;
			GameManager.gm.player.caracsPoints--;
		}
	}

	public void con() {
		if (GameManager.gm.player.caracsPoints > 0) {
			GameManager.gm.player.stats ["con"].value++;
			GameManager.gm.player.caracsPoints--;
		}
	}

	public void armor() {
		Debug.Log("popo");
		if (GameManager.gm.player.caracsPoints > 0 && GameManager.gm.player.stats ["armor"].value < 150) {
			GameManager.gm.player.stats ["armor"].value++;
			GameManager.gm.player.caracsPoints--;
		}
	}*/
	
}
